# Network Configuration

# 3D basic-topology + ExpanderGraph for MoE
topology: [ FullyConnected, FullyConnected, FullyConnected, Ring ] 

# 2 x 8 x 4 x 2 = 128 NPUs
npus_count: [ 2, 8, 4,  8]  # number of NPUs

# Bandwidth per each dimension
bandwidth: [ 200.0, 100.0, 50.0, 50.0 ]  # GB/s

# Latency per each dimension
latency: [ 500.0, 500.0, 500.0, 500.0 ]  # ns